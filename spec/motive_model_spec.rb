require 'spec_helper'

describe MotiveModel do
  it 'has a version number' do
    expect(MotiveModel::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
